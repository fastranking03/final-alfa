<header class="h-[80px] bg-black">
    <div class="flex justify-between gap-3 items-center px-10 pt-5">
         <div class="w-[30%]">
            <div>
                <input type="text" name="search" placeholder="Search" class="p-2 w-[100%] border-[1px] rounded-[20px] outline-none" />
            </div>
        </div>
        <div class="">
            <div>
                <% if (user) { %>
                    <a href="admin-logout" class="text-white">Logout </a>
                       <% } else { %>
                    <h2 class="text-[white] mr-0">sdsa</h2>
                <% } %>
            </div>
        </div>
    </div>
</header>